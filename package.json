{
  "name": "cvview",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^15.5.4",
    "react-dom": "^15.5.4"
  },
  "devDependencies": {
    "json-server": "^0.10.0",
    "react-redux": "^5.0.4",
    "react-scripts": "0.9.5",
    "redux": "^3.6.0",
    "reselect": "^3.0.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "start:db": "json-server --watch db.json --host 0.0.0.0 --port 3001",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "postinstall": "copy /Y db.sample.json db.json"
  }
}
